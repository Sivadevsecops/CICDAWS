version: 0.1
phases:
  build:
     commands:
         - git clone https://github.com/Sivadevsecops/CICDAWS.git
         - aws cloudformation package --template-file samTemplate.yaml  --s3-bucket pdpmalaths-lambda-pipeline  --output-template-file outputSamTemplate.yaml
artifacts:
   type: zip
   files:
     - samTemplate.yaml
     - outputSamTemplate.yaml
